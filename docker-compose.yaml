version: "3.8"

services:
  api:
    build: ./api
    ports:
      - "5000:5000"
    depends_on:
      - mongo
    environment:
      - MONGO_URI=mongodb://mongo:27017/tododb

  mongo:
    image: mongo
    ports:
      - "27017:27017"
